<UserControl x:Class="ArkForge.App.Views.ModsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Mods" FontSize="24" FontWeight="Bold" 
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding NewModId, UpdateSourceTrigger=PropertyChanged}" 
                      Padding="10" 
                      Background="{StaticResource SurfaceBrush}" Foreground="White" BorderThickness="0"
                      ToolTip="Pega aquí el ID de Steam Workshop del mod"/>

            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Text="{Binding NewModId, UpdateSourceTrigger=PropertyChanged}" 
              Padding="10" 
              Background="{StaticResource SurfaceBrush}" Foreground="White" BorderThickness="0"
              ToolTip="Pega aquí el ID de Steam Workshop del mod"/>

                <Button Grid.Column="1" Content="Agregar mod" Command="{Binding AddModCommand}" 
            Margin="10,0,0,0" Padding="15,8"
            Background="{StaticResource PrimaryBrush}" Foreground="White" BorderThickness="0"/>

                <Button Grid.Column="2" Content="Actualizar nombres" Command="{Binding RefreshModNamesCommand}" 
            Margin="10,0,0,0" Padding="15,8"
            Background="{StaticResource SecondaryBrush}" Foreground="White" BorderThickness="0"/>
            </Grid>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" 
                   Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,10"/>

        <ItemsControl Grid.Row="3" ItemsSource="{Binding Mods}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="6" Margin="0,4" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding WorkshopId}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                            </StackPanel>

                            <CheckBox Grid.Column="1" IsChecked="{Binding IsEnabled}" 
                             Content="Activo" Foreground="{StaticResource TextPrimaryBrush}"
                             VerticalAlignment="Center" Margin="0,0,15,0"/>

                            <Button Grid.Column="2" Content="Quitar" 
                                    Command="{Binding DataContext.RemoveModCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Padding="10,5" Background="{StaticResource DangerBrush}" Foreground="White" BorderThickness="0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Grid>
</UserControl>