<UserControl x:Class="ArkForge.App.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="Configuración" FontSize="24" FontWeight="Bold" 
                           Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>

                <Button Content="Guardar" Command="{Binding SaveCommand}" 
                        Margin="20,0,0,0" Padding="20,8"
                        Background="{StaticResource PrimaryBrush}" Foreground="White" BorderThickness="0"/>
            </StackPanel>

            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource SuccessBrush}" Margin="0,0,0,20"/>

            <TextBlock Text="Multiplicadores" FontSize="18" FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>

            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,15">
                    <TextBlock Text="{Binding XpMultiplier, StringFormat='Experiencia (XP): {0:0.0}x'}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <Slider Value="{Binding XpMultiplier}" Minimum="0.1" Maximum="20" TickFrequency="0.1"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="10,0,0,15">
                    <TextBlock Text="{Binding TamingSpeedMultiplier, StringFormat='Velocidad de tameo: {0:0.0}x'}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <Slider Value="{Binding TamingSpeedMultiplier}" Minimum="0.1" Maximum="20" TickFrequency="0.1"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,15">
                    <TextBlock Text="{Binding HarvestAmountMultiplier, StringFormat='Recolección de recursos: {0:0.0}x'}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <Slider Value="{Binding HarvestAmountMultiplier}" Minimum="0.1" Maximum="20" TickFrequency="0.1"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,0,15">
                    <TextBlock Text="{Binding MatingIntervalMultiplier, StringFormat='Intervalo de apareamiento: {0:0.0}x'}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <Slider Value="{Binding MatingIntervalMultiplier}" Minimum="0.1" Maximum="5" TickFrequency="0.1"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,10,15">
                    <TextBlock Text="{Binding BabyMatureSpeedMultiplier, StringFormat='Velocidad de crecimiento: {0:0.0}x'}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <Slider Value="{Binding BabyMatureSpeedMultiplier}" Minimum="0.1" Maximum="20" TickFrequency="0.1"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="1" Margin="10,0,0,15">
                    <TextBlock Text="{Binding PlayerDamageMultiplier, StringFormat='Daño del jugador: {0:0.0}x'}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <Slider Value="{Binding PlayerDamageMultiplier}" Minimum="0.1" Maximum="10" TickFrequency="0.1"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,10,15">
                    <TextBlock Text="{Binding DinoDamageMultiplier, StringFormat='Daño de dinosaurios: {0:0.0}x'}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <Slider Value="{Binding DinoDamageMultiplier}" Minimum="0.1" Maximum="10" TickFrequency="0.1"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.Column="1" Margin="10,0,0,15">
                    <TextBlock Text="{Binding StructureDamageMultiplier, StringFormat='Daño a estructuras: {0:0.0}x'}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <Slider Value="{Binding StructureDamageMultiplier}" Minimum="0.1" Maximum="10" TickFrequency="0.1"/>
                </StackPanel>

                <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,10,15">
                    <TextBlock Text="{Binding PlayerResistanceMultiplier, StringFormat='Resistencia del jugador: {0:0.0}x'}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <Slider Value="{Binding PlayerResistanceMultiplier}" Minimum="0.1" Maximum="10" TickFrequency="0.1"/>
                </StackPanel>

                <StackPanel Grid.Row="4" Grid.Column="1" Margin="10,0,0,15">
                    <TextBlock Text="{Binding DinoResistanceMultiplier, StringFormat='Resistencia de dinosaurios: {0:0.0}x'}" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <Slider Value="{Binding DinoResistanceMultiplier}" Minimum="0.1" Maximum="10" TickFrequency="0.1"/>
                </StackPanel>

            </Grid>

            <TextBlock Text="Opciones generales" FontSize="18" FontWeight="Bold" 
                       Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>

            <CheckBox Content="Permitir cámara en tercera persona" IsChecked="{Binding AllowThirdPersonPlayer}" 
                      Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>

            <CheckBox Content="Mostrar ubicación del jugador en el mapa" IsChecked="{Binding ShowMapPlayerLocation}" 
                      Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>

            <CheckBox Content="Modo PvE (sin combate entre jugadores)" IsChecked="{Binding EnablePvE}" 
                      Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>

        </StackPanel>
    </ScrollViewer>
</UserControl>